{
  "name": "@puth/dev",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:client:watch": "ts-node-dev --respawn --transpile-only client.ts",
    "dev:client": "ts-node-dev --transpile-only client.ts",
    "dev:server": "NODE_ENV=development ts-node-dev --respawn --transpile-only server.ts"
  },
  "dependencies": {
    "@puth/client": "*",
    "puth": "*"
  }
}
