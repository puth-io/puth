{
  "name": "@puth/dev",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev:client:watch": "ts-node-dev -r tsconfig-paths/register --respawn --transpile-only client.ts",
    "dev:client": "ts-node-dev -r tsconfig-paths/register --transpile-only client.ts"
  },
  "dependencies": {
    "@puth/client": "*",
    "puth": "*"
  },
  "devDependencies": {
    "tsconfig-paths": "^4.2.0"
  }
}
